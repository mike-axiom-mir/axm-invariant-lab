{
  "schema_version": "0.6",
  "module": {
    "name": "axm-invariant-lab",
    "purpose": "Bounded state-space exploration, cross-implementation checking, tractability measurement, read-only donor refinement evidence, and cross-repository counterexample packet consumption with explicit external-consumer continuity."
  },
  "capabilities": [
    {
      "id": "invariant.bounded-exploration",
      "description": "Explore a finite, explicitly declared transition model breadth-first to find shortest invariant counterexamples or preserve unknown observations as HOLD.",
      "provides": ["evidence.invariant-result", "evidence.counterexample-trace", "invariant.exploration"],
      "accepts": ["state.model", "state.transition", "invariant.predicate"],
      "tags": ["invariant", "state", "verification", "counterexample", "evidence"],
      "evidence": {
        "status": "cross_implementation_checked",
        "ceiling": "seed-evidence-rung-5",
        "boundary": "second implementation shares the authored model transition and invariant functions; model fidelity is not independently proven"
      }
    },
    {
      "id": "invariant.cross-verification",
      "description": "Cross-check the primary visited-state explorer with an independent bounded path enumerator and fail closed when the reference enumeration budget is incomplete.",
      "provides": ["evidence.cross-implementation-agreement", "evidence.exploration-divergence"],
      "accepts": ["state.model", "state.transition", "invariant.predicate", "invariant.exploration"],
      "tags": ["invariant", "verification", "cross-implementation", "evidence"],
      "evidence": {"status": "cross_implementation_checked", "ceiling": "seed-evidence-rung-5"}
    },
    {
      "id": "invariant.tractability-boundary",
      "description": "Measure deterministic work-count boundaries between the visited-state explorer and independent all-path verifier on a bounded reconverging fixture, preserving budget exhaustion as HOLD.",
      "provides": ["evidence.tractability-boundary", "evidence.verification-budget"],
      "accepts": ["invariant.exploration", "evidence.cross-implementation-agreement", "verification.budget"],
      "tags": ["invariant", "verification", "tractability", "budget", "evidence"],
      "evidence": {
        "status": "deterministic_work_count_fixture",
        "ceiling": "does-not-promote-seed-evidence-rung",
        "boundary": "synthetic reconverging model only; no wall-clock, arbitrary-model, solver, donor-runtime, or real-world performance claim"
      }
    },
    {
      "id": "invariant.refinement.monolith-pipeline",
      "description": "Read-only refinement of AXM Monolith Pipeline Fabric v0.1 candidate-only and explicit no-automatic-authority fields against a pinned donor contract fixture.",
      "provides": ["evidence.refinement-result", "evidence.authority-boundary"],
      "accepts": ["pipeline.candidate", "capability.graph"],
      "tags": ["invariant", "refinement", "monolith", "pipeline", "authority", "evidence"],
      "evidence": {
        "status": "pinned_donor_fixture_tested",
        "ceiling": "below-seed-evidence-rung-6-for-model-fidelity",
        "boundary": "separately authored adapter over an exact pinned donor contract fixture; no full-stack Monolith runtime export is claimed"
      }
    },
    {
      "id": "invariant.counterexample-packet",
      "description": "Emit bounded machine-readable FAIL/HOLD evidence without granting donor, execution, merge, promotion, or CANON authority.",
      "provides": ["packet.counterexample", "evidence.trace"],
      "accepts": ["evidence.invariant-result"],
      "tags": ["packet", "evidence", "provenance", "cross-repo"],
      "evidence": {
        "status": "cross_repo_two_external_consumers",
        "ceiling": "seed-evidence-rung-6-for-packet-consumption",
        "boundary": "AXM Monolith and AXM Profession Mesh both consume the exact v0.1 FAIL/HOLD result contract without replacing its status meaning; this does not promote model fidelity or real-environment evidence"
      }
    },
    {
      "id": "invariant.external-consumer-continuity",
      "description": "Track exact producer packet/schema blobs pinned by distinct external AXM consumers and fail closed when local producer bytes drift until compatibility is deliberately reviewed.",
      "provides": ["evidence.consumer-contract-integrity", "evidence.cross-repo-use"],
      "accepts": ["packet.counterexample", "consumer.contract-pin"],
      "tags": ["invariant", "consumer", "compatibility", "continuity", "evidence", "cross-repo"],
      "evidence": {
        "status": "two_distinct_external_consumers_pinned_and_tested",
        "ceiling": "seed-evidence-rung-6-for-packet-consumption",
        "boundary": "local compatibility guard attests producer byte identity and distinct registered repositories only; it does not remotely execute consumers or prove they are deployed"
      }
    }
  ],
  "entrypoints": [
    {"kind": "python-library", "path": "src/bounded_explorer.py", "symbol": "explore"},
    {"kind": "python-library", "path": "src/cross_verifier.py", "symbol": "cross_verify"},
    {"kind": "python-library", "path": "src/tractability_probe.py", "symbol": "run_probe"},
    {"kind": "python-library", "path": "adapters/monolith_pipeline_fabric.py", "symbol": "inspect"},
    {"kind": "python-cli", "path": "tools/run_evidence.py", "command": "python tools/run_evidence.py --check"},
    {"kind": "python-cli", "path": "tools/run_cross_verification.py", "command": "python tools/run_cross_verification.py --check"},
    {"kind": "python-cli", "path": "tools/run_monolith_refinement.py", "command": "python tools/run_monolith_refinement.py --check"},
    {"kind": "python-cli", "path": "tools/run_tractability.py", "command": "python tools/run_tractability.py --check"},
    {"kind": "python-cli", "path": "tools/check_external_consumers.py", "command": "python tools/check_external_consumers.py --check"}
  ],
  "tests": [
    {"command": "python -m unittest discover -s tests -v", "evidence": "repository-suite"}
  ],
  "truth_boundary": "A PASS means no counterexample was found only inside the exact declared model, predicates, transitions, bound, or explicit donor fields actually inspected. Cross-implementation agreement checks exploration semantics over shared authored model functions; tractability evidence is a deterministic synthetic work-count boundary, not a general performance claim; pinned donor refinement does not prove a full donor runtime. Two materially different real AXM consumers now use the exact FAIL/HOLD counterexample packet contract without semantic promotion, earning seed rung 6 only for packet/result consumption. Bounded exploration/model fidelity remains rung 5. External-consumer continuity checks producer byte identity and distinct registered repositories, not remote runtime deployment. This module grants no execution, merge, promotion, installation, device, network, or CANON authority."
}
