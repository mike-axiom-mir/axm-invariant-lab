# AXM Invariant Lab — Action Report v2.1

## Decision

**GROW / INV-10 promoted to pinned donor-fidelity + donor-owned execution refinement.**

This bounded run did not add another local fixture, solver encoding, or generic test-framework surface. `ACTION_REPORT_v2.0.md` explicitly raised the bar to a materially different evidence class. The selected advance was the named frontier: independently generated donor receipts.

## Starting state inspected

Invariant Lab `main` started at `1921f7b4255f51c3aebd556481b4b47d07818cf0` with no open PR. Its current hosted workflow was green. The preserved `RESEARCH_SEED_v0.1.md` remained provenance, not a research-only restriction.

The existing INV-10 lane already pinned Universal Creation source/test semantics, but its executable projection was still authored inside Invariant Lab. That left a transcription gap between donor runtime behavior and consumer-side evidence.

## Donor-side bounded advance

Universal Creation gained one narrow donor-owned evidence surface in PR #59:

- retained self-made fixture: `fixtures/material-donor-invariant-probe-v1.json`;
- executable producer: `tools/material_donor_invariant_receipt.py`;
- focused donor tests and truth-boundary documentation;
- no changes to `adapt_material_donor_pack` semantics.

The producer executes the real existing material-donor adapter and emits deterministic observations for:

1. complete exact adaptation;
2. unassigned-channel HOLD plus strict rejection;
3. duplicate-channel family HOLD while accepted textures survive;
4. unsupported-version fail-closed behavior;
5. explicit no-rendering and no-authority boundaries.

Universal Creation PR-head `d895cf304fb4f5e342f876179b898321acac1223` completed workflow run `34733999431` with `SUCCESS`, including the full Python 3.11 unittest suite and existing Node character-inspector routing check. PR #59 was then squash-merged as exact donor commit `b6e77afca5dce48f42c41e73af3ec2f7709d6a6d`.

Pinned donor blobs used by Invariant Lab:

- `src/axm_uc/material_donor.py` — `8e2504714825b38c07754eb90c23158c3794df51`
- `tools/material_donor_invariant_receipt.py` — `0b46d9c8e07da2ac149541b55fcac9cda26d4ad0`
- `fixtures/material-donor-invariant-probe-v1.json` — `749045f49eb9768e865782c0e582eca883e3d55a`

The only other open Universal Creation PR at donor publication time was #54, a timeout-only change for a separate pinned state-ripple regression. It was not semantic overlap with the material-donor receipt lane.

## Invariant Lab capability delta

Added:

- `adapters/universal_creation_donor_execution.py`
- `tools/run_universal_creation_donor_execution.py`
- `tests/test_universal_creation_donor_execution.py`
- `UNIVERSAL_CREATION_DONOR_EXECUTION_REFINEMENT.md`
- dedicated CI checkout/execution gate for the exact donor commit
- explicit INV-10 status promotion in `CANDIDATE_INVARIANTS.md`

The live gate first verifies exact donor Git commit and pinned blob identities, then executes the donor-owned receipt generator in the pinned checkout. Invariant Lab interprets the emitted receipt independently.

Missing required semantic evidence becomes HOLD. Provenance drift or contradictory observations become FAIL. Authority fields must remain false.

## Adversarial coverage

Eight focused new tests retain the failure boundary:

- valid donor-owned receipt PASS;
- missing required observation HOLD;
- authority escalation FAIL;
- source identity drift FAIL;
- unassigned-state silent promotion FAIL;
- duplicate-family silent acceptance FAIL;
- unsupported-version fail-open FAIL;
- donor provenance failure overrides an otherwise semantic PASS.

## Hosted evidence before final report commit

PR #22 candidate head `c4fe59c03aba4f791ed8e8d4da0d883cae940c72` completed Invariant Lab workflow run `34734239901` with `SUCCESS` on Python 3.11 and 3.13.

Python 3.11 ran **140/140 unit/refinement tests PASS**. Both matrix jobs also passed every inherited evidence/refinement/discovery/formal stage and the new donor-owned execution gate.

The live donor gate checked out exact Universal Creation commit `b6e77afca5dce48f42c41e73af3ec2f7709d6a6d`, verified all three pinned blobs, executed the donor-owned tool successfully, and returned `PASS` with no failures or unknowns. The donor-generated evidence retained source identity, explicit channel routing, HOLD semantics, unsupported-version rejection, `renderingVerified = false`, and all four donor authority fields false.

A final workflow run on the report-inclusive PR head is still required before merge; this report itself does not waive that evidence gate.

## Four-root merge gate

### Truth

The donor execution claim is exact and pinned. It does not convert a hand-authored consumer fixture into “independent runtime proof”; instead, the runtime observation is now generated by the donor repository itself and interpreted separately in Invariant Lab. Shared conceptual dependence remains named.

### Agency / non-domination

Neither donor receipt nor Invariant Lab verification grants execution, merge, promotion, installation, or CANON authority. Explicit authority fields remain false and adversarially tested.

### Continuity

The previous INV-10 source/test refinement remains intact. This run adds a second evidence class rather than silently replacing earlier semantics. Exact donor commit/blob identities make drift visible.

### Wisdom before speed

The run chose one small donor-owned probe and one live integration gate rather than growing a generic donor framework or duplicating many fixtures. The evidence rung is not promoted merely because runtime execution was added.

## Evidence ceiling

General model fidelity remains **seed rung 5 / CROSS-IMPLEMENTATION**. The counterexample packet keeps its separately earned rung-6 cross-repo status.

This run proves only the exact pinned Universal Creation material-donor adapter behavior over one retained tiny probe and deterministic mutations, plus Invariant Lab's interpretation of that donor-generated receipt.

It does **not** prove arbitrary donor packs, image semantic correctness, PBR correctness, visual quality, rendering, authorship, deployment, real-environment behavior, or constitutional/CANON authority.

## Next useful frontier

Do not add another material-donor fixture merely to increase count. A future advance should require a materially independent implementation, a real external consumer of a refinement receipt, or a concrete semantic/model defect. If those are absent and further work becomes mostly churn, HOLD/saturation is the correct outcome.
